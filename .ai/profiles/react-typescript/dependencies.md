# Dependency Policy - React TypeScript

Use the approved npm registry and package manager with a committed lockfile and frozen installs in CI.
Review licence, provenance, maintenance, advisories, bundle cost, browser compatibility and lifecycle scripts.
Prefer the approved design system and platform APIs over duplicate component/util libraries.
Treat lockfile changes and third-party scripts as security-sensitive review surfaces.
