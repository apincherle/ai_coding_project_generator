# Dependency Policy - TypeScript Node

Use the approved npm registry and package manager with a committed lockfile and frozen installs in CI.
Review licence, provenance, maintenance, advisories, transitive changes and lifecycle scripts.
Pin the package-manager version and supported Node.js engines.
Prefer platform/native capabilities over small unmaintained packages and block dependency confusion.
