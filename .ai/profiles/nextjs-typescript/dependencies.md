# Dependency Policy - Next.js TypeScript

Use the approved npm registry and package manager with a committed lockfile and frozen installs in CI.
Keep Next.js, React and hosting/runtime compatibility aligned through an approved upgrade process.
Review licence, provenance, maintenance, advisories, bundle/server impact and lifecycle scripts.
Treat middleware, authentication, observability and third-party browser scripts as high-review dependencies.
