# Dependency Policy - Scala

Use Maven Central or approved repositories through sbt/scala-cli with reproducible version resolution.
Review licence, provenance, maintenance, CVEs, eviction/conflict reports and Scala/JDK binary compatibility.
Justify compiler plugins, macros and transitive framework ecosystems.
Use dependency locking or an approved reproducibility mechanism and scan the resolved graph in CI.
