# Project Context - Rust Profile

Baseline: stable Rust, Rust 2024 edition, Cargo and the organisation-approved minimum supported Rust version.
Testing: built-in unit/integration tests, mockall only at real boundaries, proptest and Testcontainers where appropriate.
Quality: rustfmt, Clippy with warnings denied, cargo test, cargo audit/deny, coverage and Semgrep.

Replace this paragraph with the product purpose, owners, data classification, minimum supported Rust version,
target platforms, unsafe-code policy and external interfaces.
AI may implement scoped changes, but a named human owns requirements, review and release.
