name: validate starter kit

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  catalogue:
    name: validate profile catalogue
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false
      - name: Generate and validate every profile
        shell: pwsh
        run: |
          $manifest = Get-Content ./manifests/profiles.json -Raw |
            ConvertFrom-Json
          $profiles = $manifest.profiles.PSObject.Properties.Name
          $fixture = Join-Path $pwd "manifests/project-config.ci.yml"

          foreach ($profile in $profiles) {
            $destination = Join-Path $env:RUNNER_TEMP "ci-$profile"
            ./scripts/create-project.ps1 `
              -Profile $profile `
              -ProjectName "ci-$profile" `
              -Destination $destination `
              -ConfigPath $fixture
            ./scripts/validate-generated-project.ps1 -Path $destination
          }

  java:
    name: generated Java starter
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"
          cache: maven
          cache-dependency-path: templates/java/pom.xml
      - name: Generate and validate
        shell: pwsh
        run: |
          ./scripts/create-project.ps1 `
            -Profile java `
            -ProjectName ci-java `
            -Destination "$env:RUNNER_TEMP/ci-java" `
            -ConfigPath ./manifests/project-config.ci.yml
          ./scripts/validate-generated-project.ps1 `
            -Path "$env:RUNNER_TEMP/ci-java"
      - name: Verify generated Spring Boot project
        working-directory: ${{ runner.temp }}/ci-java
        run: mvn -B verify

  python:
    name: generated Python starter
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false
      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
          cache-dependency-glob: templates/python/pyproject.toml
      - name: Generate and validate
        shell: pwsh
        run: |
          ./scripts/create-project.ps1 `
            -Profile python `
            -ProjectName ci-python `
            -Destination "$env:RUNNER_TEMP/ci-python" `
            -ConfigPath ./manifests/project-config.ci.yml
          ./scripts/validate-generated-project.ps1 `
            -Path "$env:RUNNER_TEMP/ci-python"
      - name: Install approved dependencies
        working-directory: ${{ runner.temp }}/ci-python
        run: uv sync --locked --all-groups
      - name: Verify generated Python project
        working-directory: ${{ runner.temp }}/ci-python
        run: |
          uv run ruff format --check .
          uv run ruff check .
          uv run mypy src
          uv run pytest

  csharp:
    name: generated C# starter
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: "9.0.x"
      - name: Generate and validate
        shell: pwsh
        run: |
          ./scripts/create-project.ps1 `
            -Profile csharp `
            -ProjectName ci-csharp `
            -Destination "$env:RUNNER_TEMP/ci-csharp" `
            -ConfigPath ./manifests/project-config.ci.yml
          ./scripts/validate-generated-project.ps1 `
            -Path "$env:RUNNER_TEMP/ci-csharp"
      - name: Restore approved dependencies
        working-directory: ${{ runner.temp }}/ci-csharp
        run: dotnet restore
      - name: Verify generated C# project
        working-directory: ${{ runner.temp }}/ci-csharp
        run: |
          dotnet format --verify-no-changes --no-restore
          dotnet build --no-restore --warnaserror
          dotnet test --no-build --no-restore
