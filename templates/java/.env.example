# Local development only. Copy to `.env` and change values before sharing environments.
# Never commit real secrets, production credentials, or client data.

APP_PORT=8080

POSTGRES_DB=customers
POSTGRES_USER=app
POSTGRES_PASSWORD=local-only-change-me
POSTGRES_PORT=5432

# Used by the Spring app when running outside Compose networking
DB_URL=jdbc:postgresql://localhost:5432/customers
DB_USER=app
DB_PASSWORD=local-only-change-me
