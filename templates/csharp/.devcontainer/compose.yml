# Dev Container override: keep the development image running for the IDE.
services:
  app:
    volumes:
      - ..:/workspace:cached
      - dev-nuget:/home/app/.nuget
    working_dir: /workspace
    command: sleep infinity
    ports:
      - "${APP_PORT:-8080}:8080"

volumes:
  dev-nuget:
