# Generated Project Governance Review

## P0 — highest leverage

- Generate `.gitignore` and `.gitattributes` for every profile.
- Emit Claude, Cursor and Copilot entrypoints that delegate to `AGENTS.md`, product context and `.ai`.
- Require every installed skill to load the generated product context file.
- Provide explicit human-run hook installation for every runnable profile.
- Apply language-neutral security CI to Java, Python and C#.

## P1 — next tier

- Validator requires ignore rules, AI entrypoints, skills context, hooks and security gates.
- Keep runtime caches and coverage artifacts out of templates.
- Generate useful `.ai/skills.md` and `.ai/prompts.md`.
- Add diagnose and refactor skill parity for Python and C#.
- Make scaffold maturity prominent in every generator interface and block claims of executable verification.

## P2 — controlled project factory

- Require project owner, namespace, data classification, risk, deployment target and approved tooling.
- Replace all sample package, module, solution and artifact identities during generation.
- Generate CODEOWNERS, SECURITY, risk-based approvals and exception records.
- Enforce coverage, dependency, secret, static-analysis and architecture gates per runnable stack.
- Add locked dependencies, SBOMs, provenance and template upgrade/version support.
